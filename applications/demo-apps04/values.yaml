# Cafe Frontend App Configuration
replicaCount: 1

image:
  repository: bobieaditya03/demo-apps04
  tag: "cd198a2"
  pullPolicy: Always

nameOverride: "demo-apps04"
fullnameOverride: "demo-apps04"

service:
  type: ClusterIP
  port: 80
  targetPort: 3000

ingress:
  enabled: true
  className: traefik
  hosts:
    - host: hotel.local
      paths:
        - path: /
          pathType: Prefix
          
persistence:
  enabled: false             # Frontend tidak perlu persistence

nodeSelector:
  node-role.kubernetes.io/worker: "true"

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

livenessProbe:
  httpGet:
    path: /health
    port: 3000
  initialDelaySeconds: 10
  periodSeconds: 30

readinessProbe:
  httpGet:
    path: /health
    port: 3000
  initialDelaySeconds: 5
  periodSeconds: 10
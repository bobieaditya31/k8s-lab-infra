replicaCount: 2

image:
  repository: bobieaditya03/myapp
  tag: v10

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: nginx
  host: myapp.local
  path: /

persistence:
  enabled: true
  size: "5Gi"
  storageClass: nfs-client
  accessMode: ReadWriteOnce